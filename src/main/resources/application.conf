ktor{

    application{
        modules = [ com.example.ApplicationKt.module ]
    }

    deployment {
        port = 8081
    }
}

postgres {
  url: "jdbc:postgresql://localhost/tasks"
  user: "postgres"
  driver: "org.postgresql.Driver"
  password: "ion23#r7"
  domain: "https://jwt-provider-domain/"
  audience: "jwt-audience"
  realm: "ktor sample app"
}